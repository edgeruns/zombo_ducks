apiVersion: v1
kind: Service
metadata:
    labels:
        app: zombofight-ui
    name: zombofight-ui
    namespace: games
spec:
    ports:
        - port: 3333
          name: http
          protocol: TCP
          targetPort: 3333
    selector:
        app: zombofight-ui
    sessionAffinity: None
    type: ClusterIP
