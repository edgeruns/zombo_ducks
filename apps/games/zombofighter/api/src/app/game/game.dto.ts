export class MoveDto {
    damage: {
        head: boolean
        body: boolean
        legs: boolean
    }

    protection: {
        head: boolean
        body: boolean
        legs: boolean
    }
}
